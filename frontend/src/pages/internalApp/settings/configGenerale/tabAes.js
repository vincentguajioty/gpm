import React from 'react';
import { Card, Tabs, Tab } from 'react-bootstrap';

const ConfigGeneraleTabAES = ({
    config,
    setPageNeedsRefresh,
    isLoading,
    setIsLoading,
}) => {
    return (<>
        <li>aesFournisseurTemoin</li>
    </>);
};

ConfigGeneraleTabAES.propTypes = {};

export default ConfigGeneraleTabAES;
